all:
	cd lib && $(MAKE)
	if [ @has_fortran@ = yes ] ; then cd libf && $(MAKE) ; fi

distclean: clean
clean:
	cd lib && $(MAKE) clean
	cd libf && $(MAKE) clean

install:
	cd lib && $(MAKE) install
	if [ @has_fortran@ = yes ] ; then && cd libf && $(MAKE) install ; fi
