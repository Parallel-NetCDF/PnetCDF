mpiexec -n 4 mcoll_perf  //test the blocking I/O and non-blocking I/O

The default path to write output is "./". You can change the path and output file names in mcoll_perf.c: "strcpy(basename,  "./test")".
