#
# Copyright (C) 2013, Northwestern University and Argonne National Laboratory
# See COPYRIGHT notice in top-level directory.
#
# $Id$
#
# @configure_input@

srcdir = @srcdir@
VPATH  = @srcdir@

include ../../macros.make

INCLUDES  = -I../../src/lib
FPPFLAGS += -I../../src/libf -I../../src/libf90
LDFLAGS  += -L../../src/lib
LIBS     += -lpnetcdf

C_SRCS   = test_subfile.c

CXX_SRCS = 

F77_SRCS = 

PROGS    = $(C_SRCS:.c=)
OBJS     = $(C_SRCS:.c=.o)
ifeq (@has_mpicxx@, yes)
PROGS   += $(CXX_SRCS:.cpp=)
OBJS    += $(CXX_SRCS:.cpp=.o)
endif

ifeq (@has_fortran@, yes)
PROGS   += $(F77_SRCS:.f=)
OBJS    += $(F77_SRCS:.f=.o)
endif

GARBAGE      = $(PROGS) *.nc
PACKING_LIST = $(C_SRCS) $(F77_SRCS) $(CXX_SRCS) Makefile.in depend README

all: $(PROGS)

test_subfile: test_subfile.o $(LIBRARY)
	$(LINK.c) $< $(LDFLAGS) $(LIBS)

ptest : $(PROGS)
	$(TEST_MPIRUN) -n 2 ./test_subfile -f $(TEST_OUT_DIR)/test_subfile.nc -s 2

include $(srcdir)/../../rules.make
include $(srcdir)/depend

